<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="me.hypertesto.questeasy" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme" >
        <activity android:name=".activities.HomeActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.EditDecActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/insertDcard" />
        <activity android:name=".activities.NewCardActivity" />
        <activity android:name=".activities.FormGuestActivity" >
        </activity>
    </application>

</manifest>
